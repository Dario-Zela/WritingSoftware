<ListViewItem x:Class="Blaze.UIElements.CustomMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Blaze.UIElements"
             xmlns:converter="clr-namespace:Blaze.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListViewItem.Resources>
        <converter:ColorIntensityConverter x:Key="ColorIntensityConverter" />
    </ListViewItem.Resources>
    <ListViewItem.Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Button Content="{TemplateBinding Content}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Command" Value="{Binding Command, 
                                                        RelativeSource={RelativeSource AncestorType=local:CustomMenuItem} }"/>
                                    <Setter Property="CommandParameter" Value="{Binding CommandParameter, 
                                                        RelativeSource={RelativeSource AncestorType=local:CustomMenuItem} }"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type Button}">
                                                <!-- Background -->
                                                <Border x:Name="border"
                                                        Background="{TemplateBinding Background}"
                                                        BorderThickness="0"
                                                        SnapsToDevicePixels="true"
                                                        Cursor="Hand"
                                                        Padding="{Binding ItemMargin, 
                                                                RelativeSource={RelativeSource AncestorType=local:MenuContent} }">
                                                    <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <!-- Hover Color -->
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" TargetName="border" Value="{Binding HoverColor, 
                                                                                                                    RelativeSource={RelativeSource AncestorType=local:MenuContent},
                                                                                                                    Converter={StaticResource ColorIntensityConverter}, ConverterParameter=0.4|250}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ListViewItem.Style>
</ListViewItem>
