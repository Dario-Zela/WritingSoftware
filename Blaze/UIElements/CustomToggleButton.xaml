<ToggleButton x:Class="Blaze.UIElements.CustomToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Blaze.UIElements"
             mc:Ignorable="d" 
             xmlns:converter="clr-namespace:Blaze.Core"
             d:DesignHeight="450" d:DesignWidth="800">
    <ToggleButton.Resources>
        <converter:ColorIntensityConverter x:Key="ColorIntensityConverter" />
    </ToggleButton.Resources>
    <ToggleButton.Style>
        <Style TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid>
                            <!-- Background -->
                            <Border x:Name="border"
                                     Background="{TemplateBinding Background}"
                                     BorderThickness="{TemplateBinding BorderThickness}"
                                     BorderBrush="{TemplateBinding BorderBrush}"
                                     CornerRadius="{Binding CornerRadius, 
                                                    RelativeSource={RelativeSource AncestorType=local:CustomToggleButton}}"
                                     SnapsToDevicePixels="true"
                                     Cursor="Hand"
                                     Padding="{TemplateBinding Padding}"/>
                            <ContentPresenter Content="{TemplateBinding Content}"
                                              
                                              x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- On Mouse Effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="{Binding HoverColor, 
                                                                                         RelativeSource={RelativeSource AncestorType=local:CustomToggleButton},
                                                                                         Converter={StaticResource ColorIntensityConverter}, ConverterParameter=2|250}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </ToggleButton.Style>
</ToggleButton>
